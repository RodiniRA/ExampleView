<UserControl x:Class="AmTrustExample.UserControls.DropDownButtonItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="DropDownButtonItemRoot">
        <Button x:Name="thebutton" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"  Style="{DynamicResource DropDownButtonItemStyle}">
            <StackPanel Orientation="Horizontal" >
                <Image x:Name="theImage" Source="{Binding ButtonImage}"/>
                <Label x:Name="theLabel" Margin="2,0,0,0" Content="{Binding ButtonLabel}" FontWeight="SemiBold"></Label>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
